<Window x:Class="Crono.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Crono"
        mc:Ignorable="d"
        Title="MainWindow" Height="195" Width="435">
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Label Grid.ColumnSpan="3" Content="00:00:00" FontFamily="DS-Digital" FontSize="96" HorizontalAlignment="Center" VerticalAlignment="Center"></Label>
            <Button Grid.Row="1" Grid.Column="0" Click="Button_Click">Star</Button>
            <Button Grid.Row="1" Grid.Column="1">Stop</Button>
            <Button Grid.Row="1" Grid.Column="2">Restart</Button>

        </Grid>
    </Grid>
</Window>
